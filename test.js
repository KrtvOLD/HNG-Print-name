const person = {
    id: "realLehre",
    name: "David Omolere Egbuwalo"
};

// print name in console
if(person.id === "realLehre"){
    console.log(person.name);
    alert(person.name)
} else {
    console.log("this user is not found");
}
